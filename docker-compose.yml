services:
  rts-server:
    build: .
    container_name: rts-server
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    environment:
      PORT: "8787"
      RTS_DB_PATH: "/app/data/rts.db"
    volumes:
      - ./data:/app/data
